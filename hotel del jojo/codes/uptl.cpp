#include <stdio.h>
#include <stdlib.h>
#include <cstring>
#include <string.h>
#include <ctype.h>
#include <cctype>
int main(){
    // validasi string lower and upper 
//   char ch[20];
//   int i = 0;
//       do{
//           printf("masukkan nama (lower case) : ");
//           scanf("%s", ch);
//       }while(ch[i] != tolower(ch[i]));

    //Validasi input string
    // char p [101]= {"Pantai"};
    // char x [101]= {"Bugil"} ;
    // char name[101];
    // int i = 0;
    // do{
    //     printf("Ena ena :");
    //     scanf("%s", name);
    // }while(name[i] != p[i] && name[i] != x[i]);

    return 0;
}